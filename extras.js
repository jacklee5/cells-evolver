// unused code that I might want later

// create walls
// const walls = [
//     Bodies.rectangle(-BORDER_THICKNESS / 2, SIM_HEIGHT / 2, BORDER_THICKNESS, SIM_HEIGHT, {isStatic: true}), // left
//     Bodies.rectangle(SIM_WIDTH + BORDER_THICKNESS / 2, SIM_HEIGHT / 2, BORDER_THICKNESS, SIM_HEIGHT, {isStatic: true}), // right
//     Bodies.rectangle(SIM_WIDTH / 2, -BORDER_THICKNESS / 2, SIM_WIDTH, BORDER_THICKNESS, {isStatic: true}), // top
//     Bodies.rectangle(SIM_WIDTH / 2, SIM_HEIGHT + BORDER_THICKNESS / 2, SIM_WIDTH, BORDER_THICKNESS, {isStatic: true}) // bottom
// ]
// Composite.add(this.engine.world, walls);